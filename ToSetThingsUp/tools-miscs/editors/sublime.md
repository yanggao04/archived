# Sublime Text

`command`/`control`+`shift`+`p` to open the control palette

type `package control` for the pcakges and plugins to install

[details](https://packagecontrol.io/installation) related to packages
